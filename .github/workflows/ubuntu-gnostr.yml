name: ubuntu-gnostr

# Controls when the action will run.
on:
  pull_request:
    branches:
      - '*'
      - '*/*'
      - '**'
      - 'master'
      - 'main'
  push:
    branches:
      - '*'
      - '*/*'
      - '**'
      - 'master'
      - 'main'

  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  build:
    permissions: write-all
    strategy:
      matrix:
        os: [ubuntu-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: styfle/cancel-workflow-action@0.11.0
        if: ${{ !env.ACT }}
        with:
          access_token: ${{ github.token }}
      - name: Restore deps
        id: cache-deps-restore
        uses: actions/cache/restore@v3
        if: ${{ !env.ACT }}
        with:
          path: |
            deps/*
            /root/.cargo
            /root/.rustup
            /root/.nvm
            libsecp256k1.a
          key: ${{ runner.os }}-deps

      - uses: actions/checkout@v3
        with:
          fetch-depth: '0'
          submodules: 'true'
          set-safe-directory: 'true'
      - uses: actions/setup-node@v3
        with:
          node-version: 16
      - uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
      - name: apt-get install
        run: |
          sudo apt-get update
          sudo npm install -g --force @pnpm/exe

      - name: sudo apt-get install
        run: |
          sudo apt-get install -y autoconf automake bash build-essential gettext libcurl4-openssl-dev libtool make python3 python3-pip util-linux zlib1g-dev cmake g++
          touch ~/GITHUB_TOKEN.txt

      - run: sudo make libsecp256k1.a
      - run: sudo make gnostr
      - run: sudo make gnostr-git

      - name: rust:sudo make gnostr-cat
        run: |
          sudo make gnostr-cat
      - name: rust:sudo make gnostr-cli
        run: |
          sudo make gnostr-cli
      - name: rust:sudo make gnostr-command
        run: |
          sudo make gnostr-command
      - name: rust:sudo make gnostr-grep
        run: |
          sudo make gnostr-grep
      - name: rust:sudo make gnostr-legit
        run: |
          sudo make gnostr-legit

      - run: sudo make gnostr-proxy

      - run: make cmake

      - name: Save deps
        id: cache-deps-save
        uses: actions/cache/save@v3
        if: ${{ !env.ACT }}
        with:
          path: |
            deps/*
            /root/.cargo
            /root/.rustup
            /root/.nvm
            libsecp256k1.a
          key: ${{ steps.cache-deps-restore.outputs.cache-primary-key }}

